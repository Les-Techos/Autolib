@model Autolib.Models.Domain.Borne

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Borne</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdBorne" />
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="EtatBorne" /> @Html.DisplayNameFor(model => model.EtatBorne)
                </label>
            </div>
            <div class="form-group">
                <label asp-for="Station" class="control-label"></label>
                <select asp-for="Station" class="form-control" asp-items="ViewBag.Station"></select>
                <span asp-validation-for="Station" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IdVehicule" class="control-label"></label>
                <select asp-for="IdVehicule" class="form-control" asp-items="ViewBag.IdVehicule"></select>
                <span asp-validation-for="IdVehicule" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
