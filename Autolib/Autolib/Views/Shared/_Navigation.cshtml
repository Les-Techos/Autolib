<div class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            @Html.ActionLink("Web Manga", "About", "Home", new { area = "" }, new { @class = "navbar-brand" })
        </div>

        @using Microsoft.AspNetCore.Http;


        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">Fichier<b class="caret"></b></a>
                    <ul class="dropdown-menu">
                        <li class="dropdown-header">Application</li>
                        @if (Context.Session.GetInt32("id") == null)
                        {
                            <li><a class="dropdown-item" asp-area="Connexion" asp-controller="Connexion" asp-action="Index">Connexion</a></li>
                        }

                        @if (Context.Session.GetInt32("id") > 0)
                        {
                            <li class="dropdown-header">----------</li>
                            <li role="separator" class="divider"></li>
                            <li><a class="dropdown-item" asp-area="Connexion" asp-controller="Connexion" asp-action="Deconnexion">Quitter</a></li>
                        }

                    </ul>
                </li>
                @if (Context.Session.GetInt32("id") > 0)
                {
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">Interroger<b class="caret"></b></a>
                        <ul class="dropdown-menu">
                            <li class="dropdown-header">Interroger</li>
                            <li role="separator" class="divider"></li>
                            <li><a class="dropdown-item" asp-area="Liste des Mangas" asp-controller="Manga" asp-action="Index">Liste des Mangas</a></li>
                            <li><a class="dropdown-item" asp-area="Manga par genre" asp-controller="Manga" asp-action="Genre">Manga par genre</a></li>
                        </ul>
                    </li>
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">Gérer<b class="caret"></b></a>
                        <ul class="dropdown-menu">
                            <li class="dropdown-header">Gestion</li>
                            <li role="separator" class="divider">
                            <li> <a class="dropdown-item" asp-area="Ajouter Manga" asp-controller="manga" asp-action="Ajouter">AjouterManga</a></li>
                            <li>  <a class="dropdown-item" asp-area="Modifier Manga" asp-controller="Manga" asp-action="Modifier">ModifierManga</a></li>
                            <li>   <p class="dropdown-header">Manga</p> </li>
                            <li>  <a class="dropdown-item" asp-area="Augmenter Prix" asp-controller="Manga" asp-action="Augmenter">AugmenterPrix</a></li>
                        </ul>
                    </li>
                }
            </ul>
        </div>
    </div>
</div>